import { Router } from '../router.js';

export let RouterTest = function() {
    describe('Router Tests', function() { 
        it('should trigger custom onHashChange event when window hash changes');

        it('should return path value if location hash matches path pattern');

        it('should parse url parameters into object when getURLParams is called');

        it('should convert a path string to a regex when getHashRegex is called');


    });
}